<%= link_to 'Back to News List', news_items_path %>

<h1>Update News</h1>

<%= form_for(@news_item) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= render 'fields', f: f %>
  <br />
  <%= f.submit "Update", class: "btn btn-large btn-primary" %>
<% end %>